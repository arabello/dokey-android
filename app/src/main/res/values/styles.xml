<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/textPrimaryColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <!-- Splash screen -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen_bg</item>
    </style>

    <style name="SliderDialog" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <item name="android:windowMinWidthMajor">20%</item>
        <item name="android:windowMinWidthMinor">20%</item>
    </style>
</resources>
